import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

  container: {
    width: "100%",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: 'center',
    top: -320
  },
  logo: {
    width: 100,
    height: 100

  },
  CaretLeftIcon: {
    width: 30,
    color: "orange"
  }
});